/*
Crear una funcion en un archivo separado que sume
todos los numeros de un array
*/

const { sumarArray, sumarArrayAF } = require('./funciones')

array = [1,2,3,4,5]
console.log(sumarArray(array));
console.log(sumarArrayAF(array));
